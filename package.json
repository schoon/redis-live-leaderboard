{
  "name": "redis-leaderboard",
  "version": "1.0.0",
  "private": true,
  "description": "A live leaderboard backed by a Redis sorted set",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@redis/client": "^1.6.1",
    "express": "^4.21.2"
  }
}
